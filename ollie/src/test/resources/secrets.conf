secrets {

  // Shared application configuration regardless of environment
  secretKey0 = ${secretKey0}

  development {
    jira.password = ${jira.password}
  }

  production {
    jira.password = ${jira.password}
  }
}
